# pydiskwatch

Simple Python CLI to monitor and log disk space on remote VPS servers

## Install

```shell
python -m venv .venv
```

```shell
source .venv/bin/activate
```

```shell
pip install -r requirements.txt
```



